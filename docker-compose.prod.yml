services:
  template-react:
    image: ghcr.io/wellrcosta/wreislab-react-template:latest
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-fs", "http://localhost/"]
      interval: 30s
      timeout: 10s
      retries: 2
      start_period: 10s
